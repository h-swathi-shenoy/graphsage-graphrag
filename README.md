# graphsage-graphrag
A working, runnable example: an LLM-built knowledge graph in Neo4j, a GraphSAGE GNN trained on that graph, and a retriever that uses both to answer ,  multi-hop questions that plain vector RAG gets wrong.
